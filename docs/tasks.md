# tasks.md — parking-radar 백로그

진행 중/예정(`[ ]`) task만 두는 백로그다. 완료 항목은
[`docs/tasks-done.md`](tasks-done.md)에 이동하고, 현재 진척과 다음 작업은
[`docs/resume.md`](resume.md)에 기록한다. 작성 규칙은 [`docs/tasks-rule.md`](tasks-rule.md)를
따른다.

## 진행 중인 작업 인덱스

- **프로젝트 전환**
  - [ ] `T-001` — kor-travel-map식 저장소·문서·AI 작업 구조 정리
  - [ ] `T-002` — Docker Compose + PostgreSQL + Alembic 전환
  - [ ] `T-003` — 기존 SQLite/운영 API 데이터 이전 경로와 5분 무손실 컷오버
- **제품 개선**
  - [ ] `T-010` — Hallmark audit/redesign 및 반응형 UI 검증
  - [ ] `T-011` — 초기 로딩·분석 API·쿼리 성능 개선
  - [ ] `T-012` — 쿠키 기반 공항/주차장 설정 기억
  - [ ] `T-013` — 의존 라이브러리 최신화와 보안/호환성 확인
  - [ ] `T-014` — 인증 없는 내부망용 백업/복원 UI와 API
- **협업·릴리스**
  - [ ] `T-020` — 단계별 원격 커밋·Draft PR·CI 구성
  - [ ] `T-021` — 적대적 전문 리뷰 에이전트 2명 리뷰 및 수정
  - [ ] `T-022` — live E2E UI와 운영 스모크 테스트
  - [ ] `T-023` — 192.168.1.14 배포, 데이터 이전, 13번 Docker 미조작 확인

## 완료 조건

이 백로그는 코드·문서·테스트·운영 검증이 모두 끝난 뒤 각 항목을 완료 처리한다.

- [ ] PostgreSQL 컨테이너가 healthcheck를 통과하고 애플리케이션이 기동된다.
- [ ] 기존 SQLite 테스트와 PostgreSQL Docker 테스트가 모두 통과한다.
- [ ] 최근 주차 관측 구간과 마지막 수집 시각이 이전 시스템보다 늦지 않다.
- [ ] 14번에서 연속 수집이 시작되고 5분 간격의 관측 공백이 발생하지 않는다.
- [ ] 14번 공개 포트는 API `14000`, web `14001`이며 live E2E는
  `https://pr.digitie.mywire.org`에서 실행한다.
- [ ] 백업 생성·다운로드·복원 UI를 실제 브라우저에서 확인한다.
- [ ] 모바일 320/375/414px와 데스크톱 768px 이상에서 가로 스크롤·접근성 회귀가 없다.
- [ ] 두 리뷰 에이전트의 critical/major 지적이 해소되거나 근거와 함께 기록된다.
- [ ] Draft PR이 CI와 live E2E를 통과한 뒤에만 머지한다.

## 운영 제약 및 미해결 위험

- 192.168.1.13의 `digitie` 계정은 Docker 소켓 권한이 없으므로 해당 호스트의 Docker를
  조작하지 않는다. 원본 DB를 직접 읽을 수 없을 때는 기존 HTTP API의 최근 30일 관측을
  안전한 fallback으로 가져오며, exact SQLite dump가 필요하면 운영자 권한으로 별도 파일을
  제공해야 한다.
- 13번의 현재 수집기는 10분 주기로 동작 중이다. 14번은 5분 기준으로 시작하되, 공공데이터
  API rate limit과 실제 응답 시각은 `docs/architecture/collection.md`에 기록한다.
- 백업/복원 API에는 별도 인증이 없다. 인터넷에 직접 노출하지 않고 내부망 또는 외부
  게이트웨이에서 접근을 제한해야 한다.
